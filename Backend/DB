-- Accessories Table
CREATE TABLE IF NOT EXISTS accessories (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    price VARCHAR(50) NOT NULL,
    icon_name VARCHAR(100) DEFAULT 'Volume2',
    is_active TINYINT(1) DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Auto Parts Table
CREATE TABLE IF NOT EXISTS auto_parts (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    skus VARCHAR(100),
    price VARCHAR(50) NOT NULL,
    icon_name VARCHAR(100),
    type ENUM('main', 'additional') DEFAULT 'main',
    is_active TINYINT(1) DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Vehicles Table (with image support)
CREATE TABLE IF NOT EXISTS vehicles (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    price VARCHAR(50) NOT NULL,
    year VARCHAR(10),
    fuel VARCHAR(50),
    power VARCHAR(50),
    image_url VARCHAR(255) DEFAULT NULL,
    is_new TINYINT(1) DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Sample Data
INSERT INTO accessories (title, description, price, icon_name) VALUES
('Premium Audio', 'JBL, Pioneer, Alpine head units, subs, amps & speaker upgrades.', '$149', 'Volume2'),
('Custom Interiors', 'Leather seat covers, dash kits, floor liners, ambient lighting.', '$89', 'Armchair');

INSERT INTO auto_parts (title, skus, price, icon_name, type) VALUES
('Brakes & Rotors', '1,240 SKUs', '$39', 'CircleDot', 'main'),
('Batteries', '180 SKUs', '$109', 'BatteryCharging', 'main'),
('Engine Parts', '2,100 SKUs', '$19', 'Settings', 'additional'),
('Electrical', '950 SKUs', '$12', 'Zap', 'additional');

INSERT INTO vehicles (name, price, year, fuel, power, image_url, is_new) VALUES
('TOYOTA CAMRY XSE', '$34,990', '2025', '32 city', '206 hp', 'uploads/vehicles/camry.jpg', 1);